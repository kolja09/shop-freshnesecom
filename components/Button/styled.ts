import styled from "styled-components";

export const ButtonBlock = styled.a`
  padding: 6px 12px;
  background: #FFFFFF;
  border-radius: 12px;
  cursor: pointer;
	
	&:hover{
    font-weight: bold;
		transition: 0.2s;
    color: #46760A;
	}
`;